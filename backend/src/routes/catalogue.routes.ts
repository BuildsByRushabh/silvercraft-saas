import { Router } from 'express';
// Controllers to be implemented
// import * as catalogueController from '../controllers/catalogue.controller';

const router = Router({ mergeParams: true });

// TODO: Implement catalogue routes
// router.get('/', catalogueController.getCatalogueItems);
// router.post('/', requireRole('admin', 'staff'), catalogueController.createCatalogueItem);
// router.get('/:itemId', catalogueController.getCatalogueItem);
// router.patch('/:itemId', requireRole('admin', 'staff'), catalogueController.updateCatalogueItem);
// router.delete('/:itemId', requireRole('admin'), catalogueController.deleteCatalogueItem);

export default router;
